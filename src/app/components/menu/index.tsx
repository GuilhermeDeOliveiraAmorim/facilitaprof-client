import { TitleRoot } from "../title/title.root";
import { MenuButton } from "./button";
import { Root } from "./root";
import { SubTitle } from "./subtitle";

export const Menu = {
  Root: Root,
  Title: TitleRoot,
  SubTitle: SubTitle,
  Button: MenuButton,
};
